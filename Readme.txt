To get react dom into action with meteor, uninstall blaze which is there
by default


meteor remove blaze-html-templates
meteor add static-html


Cannot import images directly in meteor, they need to be in public folder.